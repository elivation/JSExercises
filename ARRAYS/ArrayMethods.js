//const names = ["kofi","ama","tina","esi","afia","janet","ama"]

// FILTER METHOD
/*function Search(array,search){
 const filtered = array.filter(value => value === search)
 console.log(filtered)
}

Search(names,"ama")*/

//MAPPING ARRAY

/*const movies=[
    {
     title:"A",
     year:2018,
     rating:2.5
    },
    {
    title:"B",
    year:2018,
    rating:3.5
    },
    {
    title:"C",
    year:2014,
    rating:4.5
    },
    ]*/
    

/*movies.map(value => 
    console.log(`
    title: ${value.title} 
    year: ${value.year} 
    rating: ${value.rating}`
    ))*/

/*movies.map(value => {
    if(value.rating > 3.0)
    console.log(`Movie ${value.title} recomended`)
})*/


//REDUCE ARRAY METHOD
/*const num = [10,29,3,45,12,78]

const sum = num.reduce((p,c) => 
{return (p+c)}
,0)

console.log(mus = [...num])*/




